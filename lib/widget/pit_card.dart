import "package:flutter/material.dart";
import "package:paddock_club/common/colors.dart";

class PitCard extends StatelessWidget {
  const PitCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Card();
  }
}
